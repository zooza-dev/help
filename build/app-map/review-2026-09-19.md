# Settings screens — freshness review, 2026-09-19

Account: Playfulmotion, uk.zooza.app. 23 Settings subpages walked by
`scripts/screenshots/app_map.py explore`; every one rendered (no 404s).
Shots in `build/app-map/shots/`, page text in `build/app-map/text/`.

## A. Bugs in the app (fix in ../app, not in the demo account)

| Where | What | Source |
|---|---|---|
| Settings → Other → Zooza Sites | Page title reads **"Zooza Sitesx"** | `lib/str_en3.js:4192` `settings__zooza_sites__title` |
| Settings → Billing → Invoices | **Mojibake "â‚¬50 / â‚¬70 / â‚¬20"** instead of € in the auto-invoicing intro | `str_en3.js:2449` `settings__general__invoices__intro`; same bug at `:3801` `merge_vars__course_price__help` |
| Settings → Billing → Invoices | **Raw i18n keys** `settings__engine_accounts__clear / last_synced / resync` shown in the Xero line-type config — the keys are used by `components/invoice_line_type_config/` but registered in **no** locale file | needs `/add-translations` |
| Settings → Billing → Invoices | "sync invoices.Xero will" — missing space after the full stop | `settings__*xero*` intro string |

## B. Demo data to tidy (in the Playfulmotion account)

| Screen | Problem | Suggested fix |
|---|---|---|
| Access (team) | Owners "Brian James" / "Brain James" — **kept**, that is the demo persona (decided 2026-09-19). Their @zooza.online addresses are masked to demo@playfulmotion.co.uk by capture.py (`emailReplacement` in screenshot-mask.json) | nothing to do in the account |
| Access (team) | Member with **no name**: "(Dan@playfulmotion.co.uk)" | give a name or delete |
| Consents & Agreements | Consents named **"Yes or no", "Pho", "Order"** | rename: "Photo consent", "Terms & conditions", … |
| Labels | Labels **"label", "New Label", "Payment follow-up 1"** | rename to real-looking labels (e.g. "Waiting list", "Sibling discount") |
| Invoice profiles | Default profile named **"Name"**; third one **"New Invoice Profile"**, both with Not Set IDs | rename to "Playfulmotion Ltd" / delete the stray one |
| Venues | First three rows **"My", "Another", "Online"** with junk addresses | deactivate |
| Notification center | Only notification points to michal@zooza.online — masked to demo@playfulmotion.co.uk at capture time | nothing to do |
| Metadata keys | Empty state | add one key (e.g. "Level") from a programme |
| Custom contact fields | Empty state | add one field (e.g. "Child's age") |
| Shared sessions | Empty state (BETA) | fine to keep empty, but one set would make a better picture |

## C. Good as they are

General, Contact forms, Custom Holidays, Billing periods, Payment settings,
Payment schedule templates, Payment collection setup, Make-up sessions,
Instructors rates, MATKO, Trash, Exports.
Xero is connected and valid until 18 Nov 2026 — the `invoices-list.png` shot
marked `blocked` in `manifest.yml` can probably be re-tried.

## D. Tooling notes

- The app scrolls inside a container, so Playwright `full_page` returns the
  viewport only. Use a tall viewport (1600×2600 worked for Team) for long pages.
- `explore` does not mask PII — it is a review pass. Publishing still goes
  through `capture.py`, which masks `@zooza.online`.
- Detectors that fired: raw i18n key (1 screen). Added since: mojibake, empty state.

## E. Navigation drift fixed in the KB (2026-09-19)

Decision: demo-data cosmetics (section B) are not a priority — navigation
paths and functional explanations are. Compared every "Settings → …" cue in
`content/` against the real menu from `app-map.yml`:

| KB said | Menu says | Fixed |
|---|---|---|
| Settings → Team / Users / Team & Settings → Access | Settings → General → Access | 21 |
| Settings → Locations / Places / Holidays / Courses | Settings → General → Venues / Custom Holidays / Metadata keys | 12 |
| Settings → Payments / Payment Settings (grace window, notification toggles) | Settings → Billing & Payments → Payment settings | 11 |
| Settings → Payments / Payment Templates (templates) | Settings → Billing & Payments → Payment schedule templates | 7 |
| field "Number of days until payment is due" | field "Days before booking is marked unpaid" | 6 |
| Settings → Billing Profiles / Invoice Profiles / Invoice Settings / Billing Periods | … → Invoice profiles / Invoices / Billing periods | 11 |
| Settings → Make-up sessions / Consents / Contact forms / Notifications (no group) | … → Other / General → … | 16 |
| "Settings → General → Attendance/Availability management settings for instructors" | those fields live on Access, not General | 2 |
| "Settings → Programmes → Set a limit for cancelling…" | Settings → General → General → Settings for programmes | 3 |
| "Inbound payments setup → Payment acceptance policy" | Payment settings → Payment acceptance policy | 1 |

36 articles touched, `last_converted` bumped on each. Programme-level Settings
(Price and Payment, Trial, Online registration, Auto-enrolment) were left alone —
different menu. Glossary files not edited (derived from sdd-workflow).

Left unverified: `setup/billing-and-invoicing.md:350` "Settings → Current
Product → Change Subscription" — the subscription page is not in the map yet.

`python3 scripts/check_nav.py` now reports this drift; it reads the app map, so
it stays correct as long as `app_map.py build` is re-run when the app changes.

## F. Screen field reference (2026-09-19)

`app_map.py fields` reads every Settings form straight out of the DOM — label,
control type, options, and the help text under the field — including the Add
dialogs on list screens. 23 screens → 55 cards → 131 distinct fields, written to
`build/app-map/screens/<id>.json` and `build/exports/agent/screens.jsonl`
(same record shape as canonical.jsonl; `type: screen`, `heading_path` = card).
`scripts/deploy/build_embeddings.py` now loads it alongside the other two.

**Deep crawl (`fields --deep`, later the same day):** every safe button and
row link on each screen is clicked from a fresh load and whatever opens is read
too — Edit dialogs, Configure panels, the payment-template form, contact-form
detail, venue edit, Zooza Sites page editors. 23 screens → 79 distinct cards →
159 distinct fields (from 131). It also found 40-odd sub-screens the map did not
have (`settings/contact_forms/new`, `settings/payment_schedule_templates/:id`,
`settings/team/add`, `trainers/:id/trainer_settings`, `me`, `subscription`…);
the last two are exported as their own screens. Deny-list keeps it away from
Save/Delete/Disconnect/Sync/Restore and the payment-setup wizard.

34 of the 159 fields are never named anywhere in `content/` — the assistant had
nothing to answer from. The full list is above in the session log; the
biggest clusters are the Payment settings notification toggles (5), the
Invoices auto-generation switches (4) and three Access-page instructor
permissions.

Not covered yet: Labels and Metadata keys (edit-only, no Add button), Zooza
Sites (Edit dialogs), Trash and Exports (tables, nothing to configure), and
every dialog reached by clicking a row rather than Add.

## G. Screenshot replacement, Settings (2026-09-19)

`image_index.py` placed 92 of the 209 Settings-related images on a mapped
screen (the rest are programme settings, integrations and widgets — next
areas). It proposed a `capture.py` manifest; every old/new pair was reviewed on
contact sheets (`contact_sheet.py`). Lesson from the first pass: 43 of 59 old
pictures are the whole page with the menu, not a card close-up, so `card:` now
means "scroll the page to this card" and `card_only: true` is the exception.

| Decision | Count | Why |
|---|---|---|
| **Replaced** | 38 | screen matched; new picture shows current UI and menu names |
| Kept old | 47 | engine-specific (Abra Flexi, Fakturoid, Oblio, SmartBill, Számlázz — demo runs Xero), programme-level pages, or already current English |
| Blocked | 4 | app bugs visible in the new shot: "Zooza Sitesx", mojibake "â‚¬", raw `settings__engine_accounts__*` keys |
| Deleted | 1 | `client-import-01.png` was a 32×32 black square referenced from 16 places |

32 articles updated: descriptive alt text on every replaced image (most had
"Screenshot — …"), `last_converted` bumped, `needs_screenshot_replacement`
cleared on the two articles whose every image was replaced.

Two passages were wrong about the product, caught only because the new picture
contradicted them: the company logo is uploaded (drag & drop), not set by URL
(`reference/communication-send-email.md`); "Automatically pair payments only for
unpaid bookings" is now "Only reconcile payments to outstanding bookings" under
Payment collection setup (`guides/payment-pairing.md`).

The 4 blocked shots re-run with `capture.py --status blocked` once the strings
are fixed in `../app`.

## H. Programmes, Classes, Sessions, Documents library (2026-09-20)

Map grew by 43 screens: programme list/create/overview/settings + 14 settings
tiles (`courses/:id/settings?edit=<tile>`), automations, new class, copy; class
list, class detail + 12 panels (`courses/schedules/:id?edit=<panel>`), copy;
sessions list and detail; and the Documents library (`files`, add new, document,
dynamic document, video). Representative records: programme 150 (Trailblazer
Tumblers), class 230, session 22131. Explore: all screens render; one empty
state. `fields --deep`: **202 cards, 502 fields** in `screens.jsonl` across
Settings (79 cards), Programmes (60), Classes (51), Documents (12).

Navigation: 35 programme-level cues normalised to the tile names the app
uses — **Price and payment**, **Online booking**, **Auto-enrolment**, **Trial**,
**Programme settings** (Labels live there; there is no "Labels and colours"
tile); "Programme Settings → Edit / Payment" (14) rewritten to the tile. The
header user menu (your name, top right → My profile / Subscription) replaced
"Settings → My profile" (11) and "Settings → Current Product". `check_nav.py`
now checks programme tiles too and reports 0.

Dictionary: 15 stale navigation paths fixed in the master
`sdd-workflow/translations/terminology.yml` (Locations → Venues, Notifications
→ Notification center, Retention → Auto-enrolment, Make-up sessions under
Other, Price and Payment casing, My profile and Subscription via the header
menu); `sync_terminology.py` regenerated the derived copy.

Images, flagged-articles slice: 95 old/new pairs reviewed (documents.md alone
had 25). **22 replaced**, 71 kept (client zone, booking detail, session
attendance, reports, calendar — areas not mapped yet, or data-dependent states
such as an expanded tile), **2 black 32×32 placeholders deleted**
(`discount-code-01.png`, `client-import-03.png`, the latter referenced from six
articles). 18 articles updated. Hit rate is lower than Settings because the
flagged articles are mostly about the client zone and bookings; those areas
come next.

## I. Bookings, Clients, Orders, Contacts (2026-09-21)

Map +22 screens: bookings list/create/detail with 5 tabs and 6 action dialogs
(change status, cancel, move, copy, transfer to network, add credit), clients
list/import/detail, orders list/detail/payments, contacts list/detail.
Representative records: booking 1486 (Mark Lambracht, enrolled), client 1951.
`fields --deep` → `screens.jsonl` now **300 cards** (Bookings 71, Clients 27).
PII scrub added to the export: any email/phone/IBAN in a card becomes a
placeholder — the demo account's own test addresses had leaked into one card.

Two things the crawl found: the bookings list carries a saved filter
(*Status: Auto-unenrolled*) that hides everything until Reset; the invoices
tab of a booking shows Xero "Token refresh failed" rows (already known).

Images: 78 pairs reviewed, **6 replaced** (bookings list filtered by programme,
booking tile, credits tab, cancel and change-status forms), 71 kept, 1 black
placeholder deleted (`allowing-multiple-registration-03.png`, 3 references).
The low hit rate is the demo data, not the tool: the clients list opens on
"No name" test accounts, the newest bookings are the owner's own test
bookings, the representative booking has no payment plan, and most flagged
images in these articles are the client zone (parent-facing), which the map
does not cover. Emails masked: `@gmail.com` and the demo's fake domain added.

Next areas: Payments (list, refunds, reconciliation, invoices), Communication,
Calendar, then the client zone as its own account.
