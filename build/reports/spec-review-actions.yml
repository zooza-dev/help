# Auto-generated by kb:spec-review — consumed by kb:spec-apply
generated: "2026-05-11"
specs_reviewed: 95

actions:

  - spec_id: "API-20260507-001"
    spec_path: "../api-v1/specs/implemented/2026-05-07-scheduled-payment-notification-config.md"
    spec_title: "Scheduled payment notification configuration — master switch + cooldown"

    update_articles:
      - path: "content/guides/automatic-payment-reminders.md"
        action: "add_section"
        section_title: "Turn off scheduled payment notifications"
        key_points:
          - "Settings → Billing → Payments has a 'Send scheduled payment notifications' toggle"
          - "When off: pre-notice, at-creation, and overdue emails all silenced"
          - "Post-charge confirmation (payment received email) is NOT affected by this toggle"
          - "Cooldown setting (months): suppresses at-creation email for Stripe stored card / GoCardless customers when a previous payment in the same series was processed within the cooldown window"
          - "Bank-transfer customers and first payment in a series always receive the email regardless of cooldown"
          - "Default: notifications on, cooldown = 0 months (no suppression)"

      - path: "content/guides/where-to-find.md"
        action: "add_row"
        table: "Settings and integrations"
        row: "Scheduled payment notification settings | **Settings → Billing → Payments**"

    questions_for_dev:
      - "What is the exact UI label for the cooldown months field?"
      - "Is there an audit log entry when a notification is suppressed by cooldown?"

    terminology_issues: []

  - spec_id: "API-20260505-002"
    spec_path: "../api-v1/specs/implemented/2026-05-05-customer-profile-consents-overview.md"
    spec_title: "Customer profile — agreements overview & on-demand PDF download"

    new_articles:
      - suggested_path: "content/guides/client-consent-overview.md"
        suggested_title: "How clients view and download their consent history"
        product_area: "Clients"
        type: "guides"
        audience: ["admin"]
        key_topics:
          - "Where clients find their consent history (Parent Zone / customer profile)"
          - "What's listed: platform consents + company consents + course consents"
          - "Yes/No/not applicable consent status per agreement"
          - "PDF download: on-demand, self-contained, includes full agreement text"
          - "Admin context: telling clients how to access this"
        questions_for_dev:
          - "What is the exact menu path in the Parent Zone?"
          - "Can admin also generate the PDF on behalf of a client from the admin side?"
          - "Are declined/not-accepted opt-in consents shown alongside accepted ones?"

    terminology_issues:
      - from: "customer"
        to: "client"
      - from: "agreements"
        to: "consents (preferred in KB, both acceptable)"

  - spec_id: "API-20260503-002"
    spec_path: "../api-v1/specs/implemented/2026-05-03-hide-segmented-schedule-dates-option.md"
    spec_title: "Registration widget option: hide_segmented_schedule_dates"

    update_articles:
      - path: "content/guides/customizing-widgets.md"
        action: "add_option"
        section: "Registration widget options"
        key_points:
          - "Option: 'Hide dates for classes with multiple segments (Blocks)'"
          - "When enabled: the parent class date is not shown on the registration tile for classes that have at least one Block"
          - "Default: off (dates shown)"
          - "Useful when: Blocks have their own dates and showing the parent class date would be confusing"
        questions_for_dev:
          - "What is the exact UI label for this option in the widget settings?"
          - "Confirm: 'segments' in spec = 'Blocks' in admin UI?"

    terminology_issues:
      - from: "schedule (in context of this option)"
        to: "class"
      - from: "segments"
        to: "Blocks (verify)"

# ── UPCOMING — do not implement until spec status = implemented ──────────────

  - spec_id: "API-20260501-001"
    spec_path: "../api-v1/specs/in_progress/2026-05-01-registration-widget-list-display-options.md"
    spec_title: "Registration widget list display options (grid/select)"
    status: upcoming

    update_articles:
      - path: "content/guides/customizing-widgets.md"
        action: "add_option"
        section: "Registration widget options"
        key_points:
          - "Course list display: 'select' (dropdown, default) or 'grid' (visual card tiles)"
          - "Columns (grid only): 1–4 columns"
          - "Grid view gives clients a visual programme browser instead of a dropdown"

  - spec_id: "API-20260506-001"
    spec_path: "../api-v1/specs/in_progress/2026-05-06-ad-hoc-payment-auto-process-past-dates.md"
    spec_title: "Ad-hoc scheduled payment — auto-process past-dated entries"
    status: upcoming

    update_articles:
      - path: "content/guides/ad-hoc-scheduled-payment.md"
        action: "add_note"
        key_points:
          - "If due date is in the past: payment is processed immediately and silently on creation"
          - "No customer-facing notification fires for past-dated ad-hoc payments"
          - "The payment appears as 'processed' immediately — no second step needed"
